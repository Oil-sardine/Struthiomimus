#ifndef DIFF_H
#define DIFF_H

class diff {
    private:
    unsigned long time_ms2_old; //前回時間(運動量算出用)
    unsigned long time_ms2_new; //現在時間(運動量算出用)

    float rps_rad_old; //前回角速度(rad)
    float rps_rad; //現在角速度(rad)

    public:
    

};

#endif